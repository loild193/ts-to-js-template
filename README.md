# Typescript to Javascript Template

Use template to write scripts, backend code, ... in Typescript and compile into Javascript

### Commands:

- Build:
```sh
pnpm build
```

- Using `node` with file path `./dist/...` to run file script